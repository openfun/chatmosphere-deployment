#!/usr/bin/env bash

set -eo pipefail

# Run Terraform commands in the docker container passing our environment variables

docker run --rm -it \
    -u "$(id -u)" \
    -v "${PWD}/terraform:/app" \
    -w "/app" \
    --env-file ./env.d/terraform \
    "hashicorp/terraform:0.14.5" \
    "$@"
